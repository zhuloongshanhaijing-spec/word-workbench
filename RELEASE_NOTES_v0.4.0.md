# 每日录词工作台 v0.4.0

## 本版重点：本机智能义项推荐

- 新建 Unit 时可填写学科、主题和可选的课程说明；中文主题可直接使用，例如“生物、细胞膜、代谢”。
- 可在“设置”的“回退 1：Ollama bge-m3（可选）”分组统一管理 Ollama embedding 模型。默认模型为 `bge-m3`（约 1.2GB）。
- 模型仅计算“本 Unit 语境—已存在候选义项”的相关性；它不会生成、翻译或改写词典释义、例句、搭配。
- 程序用当前语义引擎的校准相关性分数对义项排序，并只默认勾选超过保守阈值（置信下限与领先幅度）的高相关项；Open Dictionary 领域标签和 `core/common/rare` 等级用于透明的词典标签规则回退与推荐理由展示。
- 每个义项展示“推荐/未推荐”和推荐理由；所有义项保留，用户仍拥有最终勾选权。
- 修改 Unit 后可点击“重新推荐本 Unit”。
- 未安装、未启动或无法连接 Ollama 时，应用按顺序回退到可选的本地 cross-encoder（若已显式安装），最后回退到透明的词典领域标签推荐；任何情况下查询、审核与 Anki 导入都不中断。

## 安装与使用

1. 安装 macOS App、Anki 与 AnkiConnect。
2. 可选安装 Ollama；在应用设置中点击“下载推荐模型（约 1.2GB）”。
3. 新建词书与 Unit，录入当天单词，审核自动勾选的义项后导入 Anki。

## 数据与隐私

Open Dictionary 数据仍为唯一的卡片事实来源，适用 CC BY-SA 4.0。Ollama 通信只发生在 `127.0.0.1`；模型分数不会写入 Anki 卡片，也不会发送到云端。完整的本机/网络行为清单见仓库 [PRIVACY.md](PRIVACY.md)。

## 源码与许可

本版本同时发布源码。仓库定位为 **source-available**（个人非商业学习/使用；商业使用需另行获得许可），最终许可证条款见仓库 README 的 License 节与 [LICENSE](LICENSE)。

## 已知限制

- 当前默认模型为官方 Ollama 库的 `bge-m3`，而不是 `multilingual-e5-small`：后者没有可直接验证的官方 Ollama 条目，导入会增加格式转换和维护风险。
- 本版未加入 OCR、真人音频、反向拼写卡或云端词典抓取。
